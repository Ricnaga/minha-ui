import type { {{pascalCase name}}Props } from "./{{kebabCase name}}.types";
import { {{pascalCase name}}Context, use{{pascalCase name}}Provider } from "./use{{pascalCase name}}";

export function {{pascalCase name}}(props: {{pascalCase name}}Props) {
  const { children, ...rest } = use{{pascalCase name}}Provider(props);
  
  return (
    <{{pascalCase name}}Context.Provider value={rest}>
      {children}
    </{{pascalCase name}}Context.Provider>
  )
}
