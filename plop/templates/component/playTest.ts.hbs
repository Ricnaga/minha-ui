import type { PlayFunction } from "storybook/internal/csf";
import { within } from "storybook/test";

export const testDefault{{pascalCase name}}: PlayFunction = async ({ canvasElement }) => {
  const canvas = within(canvasElement);
};
