import type { Meta, StoryObj } from "@storybook/react-vite";
import { use{{camelCase name}} } from "../";

const meta: Meta = {
  title: "Hooks/use{{pascalCase name}}",
  parameters: {
    layout: "centered",
  },
  tags: ["autodocs"],
};

export default meta;

export const Default: StoryObj = {
};
