import { tv, type VariantProps } from "tailwind-variants";

export const {{camelCase name}} = tv({
    base: ""
});

export type {{pascalCase name}}Variants = VariantProps<typeof {{camelCase name}}>;
